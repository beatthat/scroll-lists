{
  "name": "scroll-lists",
  "scope": "ape",
  "version": "0.0.6",
  "description": "Generic controller for a UI scrolling list of items",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/beatthat/scroll-lists.git"
  },
  "files": [
    "index.js",
    "scripts",
    "src"
  ],
  "author": "larrykirschner@gmail.com",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/beatthat/scroll-lists/issues"
  },
  "homepage": "https://github.com/beatthat/scroll-lists",
  "scripts": {
    "postinstall": "node scripts/postinstall.js",
    "install:test": "rm -f *.tgz && npm pack && cd test && npm run clean && npm install",
    "clean": "rm -f *.tgz && cd test && npm run clean",
    "sync:test2src": "rsync -rv --include '*/' --include '*.cs' --exclude '*' --prune-empty-dirs test/Assets/Plugins/${npm_package_scope}/${npm_package_name}/ ./src/${npm_package_name}/",
    "version": "git add -A",
    "postversion": "git push && git push --tags && npm run clean",
    "pub:tag": "git add -A && git commit -m ${npm_package_version} && git tag -a ${npm_package_version} -m ${npm_package_version} && git push origin master && git push origin ${npm_package_version}"
  },
  "dependencies": {
    "mkdirp": "^0.5.1",
    "ncp": "^2.0.0",
    "bindings": "github:beatthat/bindings#semver:^0.0.4",
    "binding-behaviours": "github:beatthat/binding-behaviours#semver:^0.0.12",
    "common-unityevent-types": "github:beatthat/common-unityevent-types#semver:^0.0.2",
    "controllers": "github:beatthat/controllers#semver:^0.0.4",
    "item-managers": "github:beatthat/item-managers#semver:^0.0.2",
    "rect-utils": "github:beatthat/rect-utils#semver:^0.0.3"
  }
}
